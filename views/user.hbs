<h1>HELLO!!!!</h1>

<div class="container">
    <div class="row mb-1">
        <div class="col-md-12 border">
                                <h4 class="">{{user.username}}</h4>
                                <div class="" style="flex-grow: 1;">
                                    <div>
                                        <span>MVC Tech Blog</span>
                                    </div>
                                {{!-- <button id="new-stamp-btn">New Post</button> --}}
                                </div>
                <div class="d-flex align-items-center" id="drty-card-header-textbox">
                    <div class="row ">
                        <div class="col-sm-8 flex-column">
                            <div class="bg-light bg-opacity-75 rounded p-2" style="flex-grow: 1;">
                            </div>
                            <div class="col-md-12 display-flex flex-row justify-center">
                               
                            </div>
                        </div>
                    </div>
                </div>
        </div>
    </div>
</div>

        <div class="col-md-12 border" style="margin-bottom: 10px;">
            <div class="">
                    <h3 class="">{{posts.title}}</h3>
                    <p style="font-size: xx-small;">{{posts.createdAt}}</p>
                    <h6>{{posts.text}}</h6>
                    <div>
                        {{!-- <img type="button" class="" style="width:2%" data-id={{this.id}} src="/images/trash.png"
                             alt="">
                        <img type="button" class="" style="width:2%" data-toggle="modal" data-target="#updateModal" data-post-id={{this.id}} src="/images/pencil.png"> --}}
                        
                        <img id="comment"  type="button" style="width:8%" data-toggle="modal" data-target="#commentModal"  src="/images/comment.png" comment-id={{posts.id}} > Post Comment </img>
                    </div>
                </div>
            </div>
        </div>

         <div class="modal fade" id="commentModal" tabindex="-1" aria-labelledby="commentModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content bg-dark text-light">
            <div class="modal-header">
                <h5 class="modal-title" id="commentModalLabel">Comment</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form class="update-form">
                <div class="modal-body">
                    <div class="mb-3">
                        <label for="text-update" class="form-label">Comment</label>
                        <input type="text" class="form-control" id="commentInput" placeholder="text">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-danger" id="commentBtn" data-bs-target="#updateModal">
                        Comment
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

    <script src="/js/comment.js"></script>
    <script src="/js/deleteComment.js"></script>
